git init is used to start git at the program 

git branch -m <name> is used to name the branch of your 

git status is used to see the status of your program 

git add <file1> <file2>.....  or we can use git add . to add all files

git commit -m <message> is used to remember our changes by adding comments

git push is used to push the command to github 

git log is used to see the changes made by you to the program 

git checkout <branch> is used to change the branch from one to another 

git merge <branch> is used to merge ... mann lo hm log X branch me h or command diye ki merge Y branch toh Y ko X me le ayga 

git diff <file1> <file2> gives the difference between two files 

git stash is used to checkout between files without commiting it 

